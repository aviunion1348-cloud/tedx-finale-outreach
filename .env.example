# No secrets needed. All data is bundled client-side / statically generated.
# Swap real speaker contacts by replacing public/data/speakers.json (same schema)
# then re-running: node scripts/generate-search-assets.mjs
